{
  "name": "api-proxy",
  "version": "1.0.0",
  "description": "A tiny Cloudflare Worker that holds your API-Football key server-side and proxies the handful of requests GoalHub needs, so the key is never exposed in the browser.",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
